#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
APP_ROOT="$(CDPATH= cd -- "${SCRIPT_DIR}/.." && pwd)"
LIB_DIR="${APP_ROOT}/lib"
LIBCAMERA_BIN="${SCRIPT_DIR}/catcheye-guard-bin"

export LD_LIBRARY_PATH="${LIB_DIR}${LD_LIBRARY_PATH:+:${LD_LIBRARY_PATH}}"

exec "${LIBCAMERA_BIN}" "$@"
